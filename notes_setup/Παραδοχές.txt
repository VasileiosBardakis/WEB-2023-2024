npm install package

Sto static/ mpainoun ta arxeia pou theloume na xrhsimopoihsoume mesa sta html arxeia
(express.static)


/auth is skipped and handled directly in /login

,writehead

When an account is created, it is also registered in table session
Session holds current sessionid for each account
For every action, sessionid is compared to sessionid in mysql database
if same allow, else logout and prompt to login

